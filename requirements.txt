# Task Manager API 依赖清单
# 说明：先列出后续步骤会用到的依赖，版本号在首次 `pip install` 验证后锁定。

# --- Web 框架 ---
fastapi
uvicorn[standard]

# --- 数据校验（FastAPI 自带依赖，显式声明便于锁定版本） ---
pydantic

# --- 测试 ---
pytest
httpx

# --- 代码质量（CI 中使用） ---
ruff
